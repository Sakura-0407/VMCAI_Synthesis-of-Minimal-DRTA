# DRTA统计脚本使用说明

## 📜 脚本功能
`drta_statistics.ps1` 是一个自动化PowerShell脚本，用于：

1. **自动统计DRTA状态数**
2. **提取运行时间信息**
3. **创建规范化文件夹结构**
4. **整理和移动输出文件**
5. **生成详细统计报告**

## 🚀 使用方法

### 基本用法
```powershell
# 在FlexFringe-main目录中运行
.\drta_statistics.ps1 -OutputPrefix "your_output_prefix"
```

### 完整参数
```powershell
.\drta_statistics.ps1 -OutputPrefix "timed_samples_50_drta" -InputFile "input.csv" -ResultFolder "MyResults"
```

## 📋 参数说明

| 参数 | 类型 | 必需 | 说明 | 示例 |
|------|------|------|------|------|
| `OutputPrefix` | string | ✅ | FlexFringe输出文件的前缀 | `"timed_samples_50_drta"` |
| `InputFile` | string | ❌ | 原始输入文件路径 | `"input_data.csv"` |
| `ResultFolder` | string | ❌ | 结果文件夹名称 | `"Results_Analysis"` |

## 📁 输出结构

脚本会创建以下文件夹结构：

```
{ResultFolder}/
├── 📄 DRTA_Statistics_Report.md    (详细统计报告)
├── 📂 Input_Data/                  (原始输入文件)
│   └── your_input_file.csv
├── 📂 Output_Files/                (JSON格式数据)
│   ├── {prefix}.final.json
│   └── {prefix}.init.json
└── 📂 Visualizations/              (图形文件)
    ├── {prefix}.final.dot
    ├── {prefix}.init.dot
    ├── {prefix}.final.png
    └── {prefix}.final.svg
```

## 📊 统计内容

### 自动提取的统计信息：
- ✅ **状态总数**
- ✅ **所有状态编号列表**
- ✅ **根状态样本数**
- ✅ **主要终止状态样本数**
- ✅ **运行开始和结束时间**
- ✅ **总运行时间**
- ✅ **APTA构建时间**
- ✅ **文件数量统计**
- ✅ **处理效率计算**

## 🔧 使用示例

### 示例1：最简单使用
```powershell
# 假设你刚运行完FlexFringe，产生了timed_samples_50_drta.*文件
.\drta_statistics.ps1 -OutputPrefix "timed_samples_50_drta"
```

### 示例2：包含输入文件
```powershell
# 同时移动输入文件到结果文件夹
.\drta_statistics.ps1 -OutputPrefix "timed_samples_50_drta" -InputFile "timed_samples_50.csv"
```

### 示例3：自定义结果文件夹
```powershell
# 指定自定义的结果文件夹名称
.\drta_statistics.ps1 -OutputPrefix "experiment1_drta" -ResultFolder "Experiment1_Results"
```

## ⚠️ 注意事项

1. **文件要求**：确保以下文件存在：
   - `{OutputPrefix}.final.dot` (必需)
   - `flexfringe.log` (可选，用于时间统计)

2. **权限要求**：脚本需要：
   - 读取当前目录文件的权限
   - 创建文件夹的权限
   - 移动文件的权限

3. **PowerShell版本**：建议使用PowerShell 5.0或更高版本

## 🛠️ 故障排除

### 常见错误及解决方案：

#### 错误：找不到文件
```
错误: 找不到文件 your_prefix.final.dot
```
**解决方案**：检查OutputPrefix参数是否正确，确保FlexFringe已成功运行

#### 警告：找不到日志文件
```
警告: 找不到日志文件 flexfringe.log，将跳过运行时间统计
```
**解决方案**：这是非致命警告，脚本会继续运行，但不会包含时间统计

#### 权限错误
```
Move-Item: 无法移动文件
```
**解决方案**：以管理员身份运行PowerShell，或检查文件权限

## 📈 输出示例

脚本运行成功时的输出：
```
🚀 开始DRTA统计和文件整理...
📊 统计DRTA状态数...
   ✅ 发现 36 个状态
⏱️ 分析运行时间...
   ✅ 运行时间分析完成
📁 创建文件夹结构...
   ✅ 文件夹结构创建完成
📦 整理输出文件...
   ✅ 文件整理完成
📋 生成统计报告...
   ✅ 统计报告已保存到: Results\DRTA_Statistics_Report.md

🎉 DRTA统计分析完成！

📊 关键统计数据:
   - 状态数: 36
   - 根状态样本: 50
   - 运行时间: 0.022 秒

📁 结果文件夹: Results
📄 详细报告: Results\DRTA_Statistics_Report.md
```

## 🔄 工作流集成

可以将此脚本集成到您的DRTA分析工作流中：

```powershell
# 1. 运行FlexFringe
.\build\Release\flexfringe.exe --ini ini\rti.ini --outputfile mydata_drta mydata.csv

# 2. 自动统计和整理
.\drta_statistics.ps1 -OutputPrefix "mydata_drta" -InputFile "mydata.csv"

# 3. 生成可视化
cd mydata_drta_Results\Visualizations
dot -Tpng mydata_drta.final.dot -o mydata_drta.final.png
```

---
*脚本版本: DRTA Statistics v1.0*
*更新时间: 2025年6月25日* 